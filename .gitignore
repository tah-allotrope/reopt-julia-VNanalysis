# Credentials / API keys
NREL_API.env
*.env
!*.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
coverage.xml
htmlcov/
.venv/

# Julia
*.jl.cov
*.jl.mem
deps/build.log

# IDE / AI assistants
.vscode/
.idea/
.windsurf/
.cursor/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Temporary generated reports
artifacts/reports/test_runs/*.html



# Local-only deck template cache
reports/decks/conformance/_template/

# PackageCompiler sysimage artifacts (build locally, ~200-500MB)
artifacts/sysimage/

# AI assistant worktree caches
.claude/

# Browser automation artifacts
.playwright-mcp/

# npm (used ad-hoc for pptx generation; not project code)
node_modules/
package-lock.json

# Samsung-TTC local /present run: build intermediates + local-only template backup
present/assets_local/
present/*original template*.pptx
# Stray test scratch temp (untracked from git, kept on disk)
tests/.stderr.tmp

# Large REopt solve outputs (regenerable; never tracked — only input/resolved_regime are)
**/reopt-results.json

# === Sprint 1 de-bloat (2026-06-12): regenerable pipeline outputs — local only ===
# All solve outputs, generated case reports, dashboards, and decks are reproducible
# from tracked source + data/ + scenarios/. Kept on disk, dropped from git tracking.
artifacts/
reports/*.html
reports/*.html.txt
reports/decks/
scenarios/generated/
# Keep golden references and Markdown deliverables tracked:
!examples/
# Keep repo-trim sprint execution reports tracked (deliverables tied to commits).
# Matches `...-sprint-<digit>...` (sprint-1/2/3 + final-sprint-N, any date) but NOT the
# unrelated 2026-05-19 `validation-sprint-phase-*` reports (no digit after `sprint-`).
!reports/*-sprint-[0-9]*.html

# === Sprint 1 PHASE-03: untrack foreign tooling + local-only deck output ===
# present/ decks are regenerated by scripts/python/integration/generate_samsung_ttc_deck.py
present/
# OpenCode assistant skill bundle (user tooling; keep on disk, like .claude/)
.opencode/
.opencode_http.log
# Test scratch temp
tests/.stdout.tmp

# === Sprint 2 PHASE-02: heavy reference binaries (~35MB) — untracked, kept on disk ===
# Provenance + retrieval documented in research/SOURCES.md and data/raw/saigon18/SOURCE.md.
research/TOU-Analysis_SolarBESS-ENG.pdf
research/fmp_modeling.csv
data/raw/saigon18/2026-01-29_saigon18_excel_model_v2.xlsm
